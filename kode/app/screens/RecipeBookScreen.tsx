import React from 'react';
import { View, Text } from 'react-native';

const RecipeBookScreen = ({navigation}) => {
	return(
		<>
			<View>
				<Text>Recipe Book Screen</Text>
			</View>
		</>
	);
};

export default RecipeBookScreen;
